 def dimage
pipeline {
  agent any
  stages{
    stage('package'){
       steps{
         echo "package- code"
         sh 'mvn clean package -DskipTests'
        }
      }
      stage('build'){
       steps{
         dimage = docker.build("ci-cd-demo")
         echo "image build"
       }
      }
  }

 }